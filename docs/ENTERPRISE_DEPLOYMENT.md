# Enterprise deployment: planned and deferred work

The delivered package remains a Node.js 20+ CLI with short-lived hooks, no daemon,
and no root requirement. The items below are **not implemented**.

| Status | Work and prerequisites |
| --- | --- |
| Planned | Standalone native binary without a separately installed Node.js runtime; verify all providers and hook protocols before distribution. |
| Deferred | macOS Developer ID signing, notarization, and a signed `.pkg`; require an approved Apple identity, release process, and actual verification. |
| Deferred | Windows MSI, only after Windows shell quoting, permissions, provider support, and installation/uninstallation are verified. Windows is not currently a verified deployment target. |
| Partial | Jamf and Intune deployment scripts plus a Claude Code managed-settings policy file ship in [examples/mdm](https://github.com/agent-watch-ai/agent-watch-edge/tree/main/examples/mdm), with a per-agent statement of what an administrator can and cannot lock. They install through npm, so they still depend on a Node.js runtime; the install line is the only part a signed native package changes. No configuration profile of our own is supplied, and none is needed — the fragment drops into Claude Code's `managed-settings.d/`, which is also the only agent with a managed policy file at all. |
| Planned | Separate binary delivery from managed tenant policy, credentials, and developer identity; define policy ownership, precedence, update and removal first. |
| Planned | Local signed enforcement policy, verified before use, so the blocking hook does not call the network. Current enforcement still uses a bounded network check and fails open on unexpected errors. |
| Planned | Pseudonymous developer identity using a tenant-specific HMAC; decide key provisioning, rotation, correlation and migration. Current identity is an email, not an HMAC. |
| Partial / deferred | SHA-256 checksums and an npm CycloneDX SBOM are generated by the release workflow. Signed release manifests and platform signing are deferred until credentials and verification procedures exist. Checksums alone do not authenticate a publisher. |
| Deferred | Independent penetration testing and remediation. No completed penetration test is claimed. |
| Deferred | SOC 2 readiness, evidence collection, and independent audit roadmap. No SOC 2 attestation is claimed. |

## Release operation

Ordinary pushes and PRs verify and build artifacts; they do not publish. Run
`npm ci`, the test/lint/typecheck/build checks, then `npm run release:artifacts`
to create a verified tarball, production dependency SBOM, and `SHA256SUMS`.
Verify with `sha256sum --check SHA256SUMS` from `artifacts` (macOS:
`shasum -a 256 --check SHA256SUMS`).

Publishing is a separate, explicit `workflow_dispatch` choice on `main`, gated
by the `npm-release` GitHub environment. Repository administrators must configure
environment approval rules and the npm trusted publisher for this repository,
`release.yml` workflow, and environment before use. Use npm >=11.5.1 with
Node >=22.14 for OIDC support. No long-lived npm token is required. The workflow
publishes the verified artifact with provenance; it does not create a GitHub release.
See [npm trusted publishing](https://docs.npmjs.com/trusted-publishers/).

The version must be deliberately selected before triggering publication; this
change does not bump it. Signing with a real release identity is a later step,
not a simulated signature. Package verification permits built JS/types and public
documentation only, with a 2 MB unpacked review ceiling. Hundreds of small files
are expected from the existing module layout and declarations; bundling solely
to reduce the file count is deferred.
