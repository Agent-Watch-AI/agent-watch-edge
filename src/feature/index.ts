/**
 * Feature-correlation evidence: what the working context says about which
 * piece of work a turn belongs to. Attribution itself happens in the backend.
 */
export { RE_TICKET_KEY } from './constants/feature.constants.js';
export { featureCandidatesFromBranch } from './ticket-candidates.js';
