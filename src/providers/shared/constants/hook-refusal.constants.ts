/**
 * Keys of the hook protocols themselves, as opposed to the payload fields the
 * adapters read.
 */

/** Every JSON hook protocol AgentWatch registers names its event under this key. */
export const PAYLOAD_HOOK_EVENT_KEY = 'hook_event_name';
